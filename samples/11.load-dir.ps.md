# Load Directory Example

Basic directory loading:
{{loadDir("samples/files_to_load")}}

Directory loading with ignore patterns (ignoring any .log files and temp directories):
{{loadDir("samples/files_to_load", "*.log,temp*")}}
